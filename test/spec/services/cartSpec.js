/*jshint unused: vars */
define(['angular', 'angular-mocks', 'app', 'ngStorage'], function(angular, mocks, app) {
	'use strict';

	describe('Service: Cart', function () {

		// load the service's module
		beforeEach(module('carter.services.Cart'));

		// instantiate service
		var Cart;
		beforeEach(inject(function (_Cart_) {
			Cart = _Cart_;
		}));


	});
});
